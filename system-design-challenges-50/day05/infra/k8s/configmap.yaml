apiVersion: v1
kind: ConfigMap
metadata:
  name: requirements-tracker-config
data:
  # Backend configuration
  DATABASE_URL: "postgresql://postgres:password@postgres-service:5432/requirements_db"
  REDIS_HOST: "redis-service"
  REDIS_PORT: "6379"
  
  # Frontend configuration
  VITE_API_BASE_URL: "http://backend-service:8000/api/v1"